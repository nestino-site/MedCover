/**
 * Placeholder sections for sections driven entirely by backend HTML content.
 *
 * The backend's AI pipeline produces rich HTML for these sections:
 * - Key Statistics Table
 * - AI Interview Insights Block
 * - Marketing vs Patient Reality Table
 * - Top Clinics Grid
 * - Cost Breakdown
 * - Legal Context
 * - Comparison Block
 *
 * All of these are rendered via ContentHtml using the backend's finalContent HTML.
 * In V1, the full article body renders as one ContentHtml block rather than
 * parsing individual sections — this is correct because the backend structures
 * the HTML with proper heading hierarchy and semantic sections.
 */
export {} // module marker
